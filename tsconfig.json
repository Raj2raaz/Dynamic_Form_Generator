{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "jsx": "react-jsx",
    "strict": false, //(it's okay but can enforce type checking at every level).
    "noImplicitAny": false, //(requires explicit type definitions).
    "noUnusedLocals": false,
    "noUnusedParameters": false
  }
}
